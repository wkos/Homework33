public class Maths {
    public boolean isEven(int liczba){ return liczba%2 == 0; }
    public boolean isOdd(int liczba){ return liczba%2 != 0; }
    public double circleField(double radius){
        return Math.PI * radius * radius;
    }
    public int power(int number){ return number * number; }
}
